# Руководство пользователя Butch Filename Substring Remover

## 📋 Оглавление
1. [Что это такое?](#что-это-такое)
2. [Установка](#установка)
3. [Быстрый старт](#быстрый-старт)
4. [Использование](#использование)
5. [Примеры](#примеры)
6. [⚠️ Важные предупреждения](#важные-предупреждения)
7. [Рекомендации](#рекомендации)
8. [Решение проблем](#решение-проблем)

## Что это такое?

**Butch Filename Substring Remover** — это утилита командной строки для пакетного переименования файлов и директорий. Она удаляет указанные подстроки из имён файлов и папок в заданной директории.

### Основные возможности:
- ✅ Пакетное переименование файлов и директорий
- ✅ Удаление нескольких подстрок за один запуск
- ✅ Рекурсивная обработка поддиректорий
- ✅ Вывод статистики о выполненных операциях
- ✅ Обработка ошибок с подробными сообщениями

## Установка

### Установка через pip

```bash
pip install butch_filename_substring_remover
```

### Проверка установки

После установки проверьте, что утилита доступна:

```bash
butch-rename --help
```

Вы должны увидеть справку по использованию команды.

## Быстрый старт

### Простейший пример

Допустим, у вас есть файлы:
```
/my_folder/
    document_test.txt
    image_test.png
    report_old.pdf
```

Чтобы удалить подстроку `test` из всех имён:

```bash
butch-rename /my_folder test
```

Результат:
```
/my_folder/
    document_.txt
    image_.png
    report_old.pdf
```

## Использование

### 1. Использование из командной строки

#### Базовый синтаксис:

```bash
butch-rename <путь_к_директории> <подстрока1> [подстрока2] [подстрока3] ...
```

#### Параметры:
- `<путь_к_директории>` — путь к директории, в которой нужно переименовать файлы
- `<подстрока1> [подстрока2] ...` — одна или несколько подстрок для удаления из имён файлов

#### Примеры команд:

**Удаление одной подстроки:**
```bash
butch-rename /home/user/documents _old
```

**Удаление нескольких подстрок:**
```bash
butch-rename /home/user/photos _copy _backup _tmp
```

**Использование относительных путей:**
```bash
butch-rename ./my_project test temp
```

**Использование абсолютных путей:**
```bash
butch-rename /home/user/Downloads draft
```

### 2. Использование как Python-модуль

Вы можете использовать утилиту в своих Python-скриптах:

```python
from butch_filename_substring_remover import rename_directory

# Определите параметры
root_directory = '/path/to/directory'
substrings_to_remove = ['_old', '_backup', 'copy']

# Выполните переименование
try:
    rename_directory(root_directory, substrings_to_remove)
    print("Переименование завершено успешно!")
except ValueError as e:
    print(f"Ошибка валидации: {e}")
except Exception as e:
    print(f"Произошла ошибка: {e}")
```

#### Пример с обработкой конкретных ошибок:

```python
import os
from butch_filename_substring_remover import rename_directory

root_dir = '/home/user/projects/old_project'
to_remove = ['_old', '_deprecated']

# Проверка существования директории
if not os.path.exists(root_dir):
    print(f"Директория {root_dir} не существует!")
    exit(1)

if not os.path.isdir(root_dir):
    print(f"{root_dir} не является директорией!")
    exit(1)

# Выполнение переименования
try:
    rename_directory(root_dir, to_remove)
    print("✅ Все файлы успешно переименованы")
except ValueError as e:
    print(f"❌ Ошибка: {e}")
except PermissionError:
    print("❌ Недостаточно прав для переименования файлов")
except Exception as e:
    print(f"❌ Неожиданная ошибка: {e}")
```

## Примеры

### Пример 1: Удаление временных маркеров

**Исходная структура:**
```
/project/
    main_temp.py
    utils_temp.py
    config_backup.json
    README_draft.md
```

**Команда:**
```bash
butch-rename /project temp backup draft
```

**Результат:**
```
/project/
    main_.py
    utils_.py
    config_.json
    README_.md
```

### Пример 2: Очистка скачанных файлов

**Исходная структура:**
```
/Downloads/
    document(1).pdf
    image(2).png
    video(3).mp4
```

**Команда:**
```bash
butch-rename /Downloads "(1)" "(2)" "(3)"
```

**Результат:**
```
/Downloads/
    document.pdf
    image.png
    video.mp4
```

### Пример 3: Работа с директориями

**Исходная структура:**
```
/archive/
    2024_january_backup/
        file1.txt
        file2.txt
    2024_february_backup/
        report.pdf
```

**Команда:**
```bash
butch-rename /archive _backup
```

**Результат:**
```
/archive/
    2024_january/
        file1.txt
        file2.txt
    2024_february/
        report.pdf
```

### Пример 4: Использование в скрипте

```python
#!/usr/bin/env python3
"""
Скрипт для автоматической очистки загруженных файлов
"""
from butch_filename_substring_remover import rename_directory
import os
import sys

def clean_downloads():
    downloads_dir = os.path.expanduser("~/Downloads")

    # Список нежелательных подстрок
    unwanted = [
        "(1)", "(2)", "(3)",  # Дубликаты
        "_copy", "_копия",     # Копии
        " - копия",            # Копии (русский вариант)
        "_old", "_старый"      # Старые версии
    ]

    print(f"Очистка директории: {downloads_dir}")
    print(f"Удаляемые подстроки: {', '.join(unwanted)}")

    try:
        rename_directory(downloads_dir, unwanted)
        print("\n✅ Очистка завершена успешно!")
    except Exception as e:
        print(f"\n❌ Ошибка: {e}", file=sys.stderr)
        sys.exit(1)

if __name__ == "__main__":
    clean_downloads()
```

## ⚠️ Важные предупреждения

### 🚨 Критические ограничения текущей версии:

#### 1. **Потеря регистра символов**

**Проблема:** Утилита преобразует все имена файлов в нижний регистр.

**Пример:**
```
До:  MyDocument_test.pdf
После: mydocument_.pdf  ❌ (потерян регистр)
```

**Обходное решение:** Будьте готовы к тому, что регистр букв будет потерян.

#### 2. **Удаление только в начале имени файла**

**Проблема:** Подстрока удаляется только если она находится в начале имени файла.

**Пример:**
```
Файл: document_test.txt
Команда: butch-rename . test
Результат: document_test.txt  ❌ (не изменён, т.к. "test" не в начале)
```

**Обходное решение:** Если подстрока находится в середине или конце имени, переименование не произойдёт.

#### 3. **Возможные конфликты имён**

**Проблема:** Если после удаления подстроки два файла получат одинаковое имя, произойдёт ошибка.

**Пример:**
```
До:
  report.txt
  report_old.txt

Команда: butch-rename . _old

Результат: Ошибка! Оба файла пытаются стать "report.txt"
```

**Обходное решение:**
- Проверьте содержимое директории перед запуском
- Переместите файлы, которые могут конфликтовать
- Переименуйте их вручную перед использованием утилиты

#### 4. **Защита системных директорий**

Утилита **НЕ БУДЕТ** работать в следующих системных директориях:
- `/`, `/bin`, `/boot`, `/dev`, `/etc`
- `/lib`, `/lib64`, `/proc`, `/root`
- `/sbin`, `/sys`, `/usr`, `/var`

Это сделано для безопасности системы.

### ⚠️ Общие предостережения:

1. **Нет функции отката (undo)**
   - После переименования вернуть прежние имена автоматически невозможно
   - Рекомендуется сделать резервную копию перед использованием

2. **Рекурсивная обработка**
   - Утилита обрабатывает ВСЕ файлы и папки во всех поддиректориях
   - Нет способа ограничить обработку только верхним уровнем

3. **Права доступа**
   - Убедитесь, что у вас есть права на переименование файлов
   - При работе с системными файлами может потребоваться `sudo`

4. **Специальные символы**
   - Будьте осторожны с специальными символами в подстроках
   - Используйте кавычки для подстрок с пробелами: `butch-rename . " - копия"`

## Рекомендации

### ✅ Рекомендуемый рабочий процесс:

1. **Создайте резервную копию**
   ```bash
   cp -r /original/directory /backup/directory
   ```

2. **Проверьте содержимое директории**
   ```bash
   ls -R /path/to/directory
   ```

3. **Запустите команду**
   ```bash
   butch-rename /path/to/directory substring1 substring2
   ```

4. **Проверьте результат**
   ```bash
   ls -R /path/to/directory
   ```

### 💡 Советы по использованию:

1. **Начните с тестовой директории**
   - Создайте копию директории для тестирования
   - Убедитесь, что результат соответствует ожиданиям

2. **Используйте конкретные подстроки**
   - Чем конкретнее подстрока, тем меньше вероятность нежелательных изменений
   - Избегайте коротких общих подстрок типа "a", "1", "test"

3. **Проверяйте вывод программы**
   - Утилита выводит информацию о каждом переименовании
   - Читайте сообщения об ошибках

4. **Работайте с небольшими порциями**
   - Если директория большая, разделите работу на части
   - Проще контролировать процесс и результат

## Решение проблем

### Проблема: "Permission denied"

**Причина:** Недостаточно прав для переименования файлов.

**Решение:**
```bash
# Для Linux/macOS - используйте sudo (осторожно!)
sudo butch-rename /path/to/directory substring

# Или измените права доступа
chmod -R u+w /path/to/directory
```

### Проблема: "Directory not found"

**Причина:** Указанная директория не существует.

**Решение:**
```bash
# Проверьте путь
ls /path/to/directory

# Используйте абсолютный путь
realpath ./my_folder
```

### Проблема: Не все файлы переименованы

**Причина:** Подстрока находится не в начале имени файла (известное ограничение).

**Решение:** В текущей версии обходного решения нет. Используйте другие инструменты для таких случаев:
```bash
# Альтернатива: использование rename (Linux)
rename 's/substring//' /path/to/directory/*

# Альтернатива: использование find и sed
find /path/to/directory -name "*substring*" -exec rename ...
```

### Проблема: Потерян регистр букв

**Причина:** Известная проблема текущей версии.

**Решение:** Восстановите регистр вручную или используйте альтернативные инструменты.

### Проблема: "Error: It is not safe to run this script in system directories"

**Причина:** Вы пытаетесь запустить утилиту в системной директории.

**Решение:** Это защита. Не используйте утилиту в системных директориях.

---

## 📞 Поддержка

Если вы обнаружили ошибку или у вас есть предложения:
- Создайте issue на GitHub
- Опишите проблему как можно подробнее
- Приложите примеры команд и результаты

## 📝 Лицензия

MIT License

---

**Версия документа:** 1.0
**Дата обновления:** Декабрь 2025
